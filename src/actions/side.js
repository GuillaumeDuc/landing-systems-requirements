export const setMode = mode => ({
  type: "SET_MODE",
  mode
});

export const setCSV = csv => ({
  type: "SET_CSV",
  csv
});

export const setRelation = relation => ({
  type: "SET_RELATION",
  relation
});
